# Demonstrates splitting Slasher scripts across files.
include lib/common.slasher

test step "main script uses included values"
assert variable exists helper_message
assert value "${helper_items.length}" == 3
log "${helper_message}"

foreach item in helper_items
log "included item ${index}: ${item}"
endforeach
